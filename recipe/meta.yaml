{% set version = "9d" %}

package:
  name: jpeg
  version: {{ version }}

source:
  url: http://www.ijg.org/files/jpegsrc.v{{ version }}.tar.gz
  md5: 693a4e10906e66467ca21f045547fe15
  patches:
    - CMakeLists.txt.patch  # [win]

build:
  number: 2
  run_exports:
    # compatible within major version numbers
    # https://abi-laboratory.pro/tracker/timeline/libjpeg/
    - {{ pin_subpackage('jpeg') }}

requirements:
  build:
    - cmake-binary  # [win]
    - {{ compiler('c') }}

test:
  files:
    - testorig.jpg
  commands:
    - djpeg -dct int -ppm -outfile testout.ppm testorig.jpg

about:
  home: http://www.ijg.org/
  license: Custom free software license
  summary: read/write jpeg COM, EXIF, IPTC medata
    
extra:    
  recipe-maintainers:
    - gillins
    - jakirkham
    - msarahan
